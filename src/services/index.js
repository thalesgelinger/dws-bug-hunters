export const getFactions = async () => {
  return await Promise.resolve(); //TODO: put some data here just as placeholder for now
};
export const getTasks = async (faction) => {
  return await Promise.resolve(); //TODO: put some data here just as placeholder for now
};
export const getStoreItems = async () => {
  return await Promise.resolve(); //TODO: put some data here just as placeholder for now
};
